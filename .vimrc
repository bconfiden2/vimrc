" Show line numbers
set nu

" Replace tab with spaces
set smarttab
" Set tab size
set shiftwidth=4

" New line inherits the indentation of previous line
set autoindent
set smartindent

" Always show cursor position
"set ruler

" Always display the status bar
"set laststatus=2

" Flash the screen instead of beeping on errors
"set visualbell

" Highlight current line
"set cursorline

" Hightlight search results, disable with :noh
set hlsearch
" Ignore case when searching
set ignorecase
" Incremental search that shows partial match
set incsearch
" Automatically switch search to case-sensitive, when query contains an uppercase
set smartcase
